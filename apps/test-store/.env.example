# Test Store configuration
# URL to your Orders service (default: http://localhost:4002)
ORDERS_URL=http://localhost:4002

# JWT secret shared with your app (must match services/orders JWT_SECRET)
JWT_SECRET=change-me

# Tenant ID to attribute orders to
TEST_TENANT_ID=your-tenant-id

# Optional default channel name for orders
DEFAULT_CHANNEL=test-store
